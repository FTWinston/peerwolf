import React, { useState, useEffect } from 'react';
import './Game.scss';

interface Props {
    duration: number;
}

export const Countdown: React.FC<Props> = props => {
    return (
        <div className="game game--countdown">
            blah
        </div>
    )
}